clc;
clear;
%(abs(imag(str2double(h)) > 0) || isnan(str2double(h)) || str2double(h) < 0)
a = "-3i";
aa = str2double(a);
ab = imag(str2double(a));
ac = abs(imag(str2double(a)));
ad = (abs(imag(str2double(a))) > 0);
disp(aa);
disp(ab);
disp(ac);
disp(ad);